<?xml version="1.0" encoding="UTF-8"?>
<sequence name="sendSmsSeq" trace="disable" xmlns="http://ws.apache.org/ns/synapse">
    <property name="HTTP_METHOD" scope="axis2" type="STRING" value="POST"/>
    <property action="remove" name="REST_URL_POSTFIX" scope="axis2"/>
    <header expression="get-property('ENDPOINT')" name="To" scope="default"/>
    <filter source="boolean(get-property('notifyURL'))" regex="true">
        <then>
            <sequence key="modifySendSMSRequestPayloadSeq"/>
        </then>
        <else>
            <log level="custom">
                <property name="notifyURL" value="NULL"/>
            </log>
        </else>
    </filter>

	<filter xpath="boolean($ctx:MSISDN_LIST)">
        <then>
            <!-- validate the msisdn list for black list and white list -->
            <call-template target="com.wso2telco.dep.common.blacklistWhitelistValidator.Template">
                <with-param name="paramName" value="address"/>
                <with-param name="paramValueList" value="{get-property('MSISDN_LIST')}"/>
            </call-template>
        </then>
        <else/>
    </filter>

    <call-template target="com.wso2telco.dep.common.loggingExtension.Template">
        <with-param name="direction" value="REQUESTOUT"/>
    </call-template>
    <sequence key="com.wso2telco.dep.common.change.header.name"/>
    <call>
        <endpoint key="sendSmsEp"/>
    </call>

    <call-template target="com.wso2telco.dep.common.loggingExtension.Template">
        <with-param name="direction" value="RESPONSEIN"/>
    </call-template>

    <switch source="$axis2:HTTP_SC">
        <case regex="201">
            <sequence key="sendSmsOutSeq"/>
        </case>
        <case regex="200">
            <sequence key="sendSameSmsOutSeq"/>
        </case>
        <default/>
    </switch>

    <call-template target="com.wso2telco.dep.common.loggingExtension.Template">
        <with-param name="direction" value="RESPONSEOUT"/>
    </call-template>

    <respond/>
</sequence>
